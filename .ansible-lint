---
# Ansible-lint configuration

# Exclude certain paths from linting
exclude_paths:
  - .git
  - .github
  - node_modules

# Rules to skip (commented rules are enabled by default)
skip_list:
  - command-instead-of-module  # curl commands are appropriate for installation scripts

# Verbosity level
verbosity: 1
